Show the status of the research agent.

Steps:
1. Read `state.json` and report:
   - Last run time (convert to a human-readable local time if possible)
   - Number of URLs tracked in `seen_urls`
2. List all markdown files in the `research/` directory, sorted newest first. Show filename and size.
3. Link to the most recent report file.
4. Read `config.yaml` and show the current list of sources being monitored.